"""Sales & Distribution module: quotation -> sales order -> delivery -> invoice ->
payment -> receipt, plus POS fast-sale. One shared engine over the existing inventory,
reservation, customer and ledger services. Tenant-scoped via RLS.
"""
