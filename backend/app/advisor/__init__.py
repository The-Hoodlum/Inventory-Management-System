"""AI Supply Chain Analyst (Phase 10): an explainable advisor that grounds every
recommendation in the platform's real, deterministic signals. The optional LLM
layer only *narrates* that grounded evidence — it never invents numbers."""
