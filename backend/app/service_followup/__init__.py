"""Motorcycle service follow-up: compute when a sold bike is next due for service and
list the customers to call back. Reads the serialized unit registry + an append-only
service log; writes only service records and the per-model schedule (never stock)."""
